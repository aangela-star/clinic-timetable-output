'use strict';
// Noninteractive Chrome access has not been approved. This is deliberately not a
// mutation-capable adapter. Do not turn a successful CLI launch into CMS approval.
module.exports={available:false,reason:'CHROME_PERMISSION_REQUIRED'};
